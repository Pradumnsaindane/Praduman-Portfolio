# Base URL of the API.
# LOCAL DEV: leave this blank — the Vite proxy forwards /api to http://localhost:5000.
# PRODUCTION (Vercel): set to your Render API URL WITH the /api suffix, e.g.
# VITE_API_URL=https://pradumn-portfolio-api.onrender.com/api
VITE_API_URL=
