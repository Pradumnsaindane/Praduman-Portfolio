{
  "name": "pradumn-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack MERN portfolio — React client, Express/MongoDB API with an admin dashboard.",
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n \"api,web\" -c \"green,cyan\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "seed": "npm --prefix server run seed",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
